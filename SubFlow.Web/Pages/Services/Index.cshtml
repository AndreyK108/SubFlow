@page
@model SubFlow.Web.Pages.Services.IndexModel
@using Microsoft.Extensions.Localization
@using SubFlow.Web.Localization
@inject IStringLocalizer<SharedResource> L

<div class="sf-card">
    <div class="sf-card-header">
        <div class="d-flex justify-content-between align-items-start flex-wrap gap-2">
            <div>
                <h1 class="sf-title h3 mb-1">@L["Services"]</h1>
                <div class="sf-subtitle small">@L["ServicesSubtitle"]</div>
            </div>
            <a class="btn btn-primary" asp-page="/Services/Create">@L["CreateNew"]</a>
        </div>
    </div>

    <div class="sf-card-body">
        @if (Model.Items.Count == 0)
        {
            <div class="text-muted">@L["NoItems"]</div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table sf-table align-middle">
                    <thead>
                        <tr>
                            <th>@L["Name"]</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var x in Model.Items)
                        {
                            <tr>
                                <td class="fw-semibold">@x.Name</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>
